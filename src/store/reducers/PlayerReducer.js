const INITIAL_STATE = {
    song:{}
}
export function playerReducer(state=INITIAL_STATE,action){
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}