const INITIAL_STATE = {
    currUser:{}
}
export function userReducer(state=INITIAL_STATE,action){
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}